# StudentManagementSystem
Student Management System built with Java, JDBC, Servlets, and MySQL. Features secure login, session handling, and full CRUD for student records. Uses Java Servlets with JDBC for DB operations, an intuitive HTML/CSS UI, and runs on Apache Tomcat for smooth interaction.
